# 🚀 Быстрый старт - Настройка мульти-админов

## ✅ Что уже сделано:
- ✅ Таблица `admins` создана в Supabase
- ✅ Код обновлен для поддержки мульти-админов

## 📝 Что нужно сделать сейчас:

### 1️⃣ Настроить Backend (.env файл)

**Путь:** `backend/.env`

Создайте файл с таким содержимым:

```env
PORT=3000
DATABASE_URL=postgresql://postgres:[ВАШ-ПАРОЛЬ]@db.[PROJECT-REF].supabase.co:5432/postgres
RUN_MIGRATIONS=false
```

**Как получить DATABASE_URL:**
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Ваш проект → **Settings** → **Database**
3. Найдите **Connection string** → выберите **URI**
4. Скопируйте строку
5. Замените `[YOUR-PASSWORD]` на ваш пароль базы данных

**Пример:**
```env
PORT=3000
DATABASE_URL=postgresql://postgres.abcdefgh:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
RUN_MIGRATIONS=false
```

---

### 2️⃣ Настроить Frontend (.env файл)

**Путь:** `.env` (в корне проекта `telegram-mini-app`)

Создайте файл с таким содержимым:

```env
VITE_BACKEND_URL=http://localhost:3000
```

---

### 3️⃣ Запустить Backend

```powershell
cd backend
npm install
npm start
```

**Проверка:** Должно появиться `Server running on port 3000`

---

### 4️⃣ Запустить Frontend

В новом окне PowerShell:

```powershell
cd telegram-mini-app
npm install
npm run dev
```

**Проверка:** Должен открыться браузер с приложением

---

### 5️⃣ Проверить работу

1. Откройте приложение в Telegram
2. Войдите как админ (ID: 473842863)
3. Должна открыться админ-панель
4. Нажмите **"👑 Администраторы"**
5. Должен отобразиться список с главным админом

---

## ❗ Частые проблемы:

### Backend не запускается
- Проверьте, что файл `backend/.env` создан
- Проверьте, что `DATABASE_URL` правильный
- Убедитесь, что порт 3000 свободен

### Ошибка подключения к базе данных
- Проверьте пароль в `DATABASE_URL`
- Убедитесь, что Supabase проект активен
- Проверьте формат Connection String (должен быть URI)

### Frontend не видит backend
- Проверьте, что backend запущен
- Проверьте `VITE_BACKEND_URL` в `.env`
- Убедитесь, что оба сервера запущены

---

## 📚 Подробная инструкция

Смотрите файл `COMPLETE_SETUP_GUIDE.md` для детальной информации.
