# Инструкция по интеграции режима "Экзамен"

## ✅ Что было добавлено

Режим "Экзамен" успешно интегрирован в существующее приложение **без изменения** текущего функционала раздела "Тема".

## 📋 Добавленные функции

### 1. Состояния (State)
Добавлены в `App.jsx` после строки 39:
- `activeMode` - текущий режим ('topic' или 'exam')
- `examQuestionCount` - выбранное количество вопросов
- `isExamMode` - флаг режима экзамена
- `examTimeLimit` - лимит времени в секундах
- `examTimeRemaining` - оставшееся время

### 2. Функции сбора и выбора вопросов
Добавлены после функции `getMergedQuestions`:
- `getAllQuestions()` - собирает все вопросы из всех тем
- `getRandomQuestions(allQuestions, count)` - случайно выбирает N уникальных вопросов

### 3. Обработчики событий
- `handleExamQuestionCountSelect(count)` - запуск экзамена с выбранным количеством вопросов
- `handleModeSwitch(mode)` - переключение между режимами "Тема" и "Экзамен"
- `formatExamTime(seconds)` - форматирование времени для экзамена (MM:SS)

### 4. Обновленный таймер
Таймер теперь поддерживает:
- Прямой отсчет для тестов по теме (HH:MM:SS)
- Обратный отсчет для экзаменов (MM:SS)
- Автоматическое завершение при истечении времени

### 5. Новый экран
- `examSelect` - экран выбора количества вопросов (20, 50, 100)

### 6. Панель переключения режимов
Добавлена в экраны:
- `topics` - список тем
- `topicDetail` - детали темы
- `examSelect` - выбор экзамена

## 🎨 Добавленные стили CSS

В файле `App.css` добавлены:

1. **Панель переключения режимов:**
   - `.mode-switch-panel`
   - `.mode-switch-button`
   - `.mode-switch-button.active`

2. **Таймер экзамена:**
   - `.quiz-timer-warning` - предупреждение (менее 1 минуты)
   - `.quiz-timer-expired` - время истекло
   - `@keyframes pulse` - анимация предупреждения

3. **Экран выбора экзамена:**
   - `.exam-select-container`
   - `.exam-option-button`
   - `.exam-option-count`
   - `.exam-no-questions`

## 🔧 Изменения в существующем коде

### Минимальные изменения:

1. **`handleStartTest()`** - добавлен `setIsExamMode(false)`
2. **`handleBackToTopics()`** - добавлена логика для экзамена
3. **`saveTestResults()`** - добавлена проверка `isExamMode` для сохранения результатов
4. **Таймер (useEffect)** - добавлена поддержка обратного отсчета
5. **Интерфейс теста** - добавлено отображение таймера экзамена и блокировка ответов

## 📍 Расположение кода

### В `App.jsx`:

1. **Состояния** (строки ~40-45):
   ```javascript
   const [activeMode, setActiveMode] = useState('topic')
   const [examQuestionCount, setExamQuestionCount] = useState(null)
   // ... и т.д.
   ```

2. **Функции** (после `getMergedQuestions`, ~строка 82):
   - `getAllQuestions()`
   - `getRandomQuestions()`

3. **Обработчики** (после `handleStartTest`, ~строка 143):
   - `handleExamQuestionCountSelect()`
   - `handleModeSwitch()`
   - `formatExamTime()`

4. **Экран выбора** (перед `if (screen === 'topics')`, ~строка 1027):
   - `if (screen === 'examSelect')`

5. **Панель переключения** добавлена в:
   - Экран `topics`
   - Экран `topicDetail`
   - Экран `examSelect`

### В `App.css`:

1. **Стили таймера** (после `.quiz-timer-new`, ~строка 74)
2. **Панель переключения** (после `.topics-container`, ~строка 314)
3. **Стили экзамена** (в конце файла, после `@media`)

## ✅ Проверка работоспособности

1. **Панель переключения:**
   - Откройте раздел "Тема"
   - Вверху должны быть две кнопки: "Тема" (активная) и "Экзамен"
   - Нажмите "Экзамен" - должен открыться экран выбора

2. **Выбор экзамена:**
   - На экране должны быть кнопки: 20, 50, 100 вопросов
   - При нажатии запускается экзамен

3. **Таймер:**
   - В режиме экзамена таймер показывает обратный отсчет (MM:SS)
   - При времени < 1 минуты таймер становится оранжевым
   - При истечении времени экзамен завершается автоматически

4. **Блокировка ответов:**
   - При истечении времени ответы блокируются
   - Кнопки ответов становятся неактивными

## 🔒 Гарантии

- ✅ Раздел "Тема" работает **без изменений**
- ✅ Существующие тесты **не затронуты**
- ✅ Интерфейс теста **используется как есть**
- ✅ Структура вопросов **не изменена**
- ✅ Все функции раздела "Тема" **сохранены**

## 📝 Комментарии в коде

Все добавленные функции и логика помечены комментариями:
```javascript
// ========== ЭКЗАМЕН: [описание] ==========
```

Это позволяет легко найти все изменения, связанные с экзаменом.

## 🚀 Готово к использованию

Режим "Экзамен" полностью интегрирован и готов к использованию. Все требования выполнены:
- ✅ Панель переключения режимов
- ✅ Экран выбора количества вопросов
- ✅ Случайный выбор уникальных вопросов
- ✅ Таймер обратного отсчета
- ✅ Автоматическое завершение
- ✅ Блокировка ответов при истечении времени
- ✅ Использование существующего интерфейса теста
