# Полная инструкция по настройке системы мульти-админов

## ✅ Шаг 1: Таблица admins в Supabase (УЖЕ ВЫПОЛНЕНО)

Вы уже создали таблицу `admins` в Supabase. Отлично! 

**Проверка:** Выполните в Supabase SQL Editor:
```sql
SELECT * FROM admins;
```
Должна быть запись с `telegram_id = 473842863`.

---

## 📝 Шаг 2: Настройка Backend для работы с Supabase

### 2.1. Получите Connection String из Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Database**
4. Найдите секцию **Connection string**
5. Выберите **URI** формат
6. Скопируйте строку подключения (она выглядит так):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   ```

### 2.2. Создайте файл .env в папке backend

1. Откройте папку `backend`
2. Создайте файл `.env` (если его нет)
3. Добавьте следующие строки:

```env
PORT=3000
DATABASE_URL=postgresql://postgres:[ВАШ-ПАРОЛЬ]@db.[ВАШ-PROJECT-REF].supabase.co:5432/postgres

# Если хотите, чтобы миграции запускались автоматически при старте
RUN_MIGRATIONS=false
```

**Важно:** 
- Замените `[ВАШ-ПАРОЛЬ]` на пароль от базы данных Supabase
- Замените `[ВАШ-PROJECT-REF]` на ваш Project Reference (можно найти в Settings → General)

**Как найти пароль:**
- В Supabase Dashboard: Settings → Database → Database password
- Если забыли пароль, можно сбросить его там же

**Пример готового .env:**
```env
PORT=3000
DATABASE_URL=postgresql://postgres.abcdefghijklmnop:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
RUN_MIGRATIONS=false
```

---

## 🌐 Шаг 3: Настройка Frontend

### 3.1. Создайте файл .env в корне проекта (telegram-mini-app)

1. Откройте папку `telegram-mini-app` (корень проекта)
2. Создайте файл `.env` (если его нет)
3. Добавьте:

```env
VITE_BACKEND_URL=http://localhost:3000
```

**Если backend запущен на другом адресе или порту, укажите правильный URL.**

**Пример для production:**
```env
VITE_BACKEND_URL=https://your-backend-domain.com
```

---

## 🚀 Шаг 4: Запуск Backend

### 4.1. Установите зависимости (если еще не установлены)

```powershell
cd backend
npm install
```

### 4.2. Запустите backend

```powershell
npm start
```

Или для разработки с автоперезагрузкой:
```powershell
npm run dev
```

**Проверка:** Backend должен запуститься на `http://localhost:3000`

Вы должны увидеть что-то вроде:
```
Server running on port 3000
```

---

## 🎨 Шаг 5: Запуск Frontend

### 5.1. Установите зависимости (если еще не установлены)

В корне проекта `telegram-mini-app`:
```powershell
npm install
```

### 5.2. Запустите frontend

```powershell
npm run dev
```

**Проверка:** Frontend должен запуститься (обычно на `http://localhost:5173`)

---

## ✅ Шаг 6: Проверка работы системы

### 6.1. Проверьте подключение backend к Supabase

Backend должен успешно подключиться к Supabase. Если есть ошибки подключения, проверьте:
- Правильность `DATABASE_URL` в `backend/.env`
- Что пароль в Connection String правильный
- Что Supabase проект активен

### 6.2. Проверьте админ-панель

1. Откройте приложение в Telegram
2. Войдите как главный админ (Telegram ID: 473842863)
3. Должна открыться админ-панель
4. Нажмите кнопку **"👑 Администраторы"**
5. Должен отобразиться список администраторов с главным админом

### 6.3. Проверьте API endpoints

Можно проверить через браузер или Postman:

**Проверка админ-доступа:**
```
GET http://localhost:3000/api/admin/check
Headers:
  x-telegram-user-id: 473842863
```

Должен вернуть: `{ "isAdmin": true }`

**Список администраторов:**
```
GET http://localhost:3000/api/admin/admins
Headers:
  x-telegram-user-id: 473842863
```

Должен вернуть список админов.

---

## 🔧 Решение проблем

### Проблема: Backend не может подключиться к Supabase

**Решение:**
1. Проверьте `DATABASE_URL` в `backend/.env`
2. Убедитесь, что используете правильный формат Connection String (URI)
3. Проверьте пароль базы данных
4. Убедитесь, что Supabase проект активен

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте, что backend запущен (`npm start` в папке backend)
2. Проверьте `VITE_BACKEND_URL` в `.env` файле корня проекта
3. Убедитесь, что порты не конфликтуют
4. Проверьте CORS настройки в backend (должны быть настроены)

### Проблема: Ошибка 404 при запросах к API

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте URL в `VITE_BACKEND_URL`
3. Проверьте, что роуты правильно настроены в backend

### Проблема: Админ-панель не открывается

**Решение:**
1. Проверьте, что вы вошли как правильный пользователь (ID: 473842863)
2. Проверьте, что таблица `admins` в Supabase содержит этого пользователя
3. Проверьте логи backend на наличие ошибок
4. Проверьте консоль браузера на наличие ошибок

---

## 📋 Чек-лист настройки

- [ ] Таблица `admins` создана в Supabase
- [ ] Главный админ (473842863) добавлен в таблицу `admins`
- [ ] Файл `backend/.env` создан с правильным `DATABASE_URL`
- [ ] Файл `.env` в корне проекта создан с `VITE_BACKEND_URL`
- [ ] Backend запущен и подключается к Supabase
- [ ] Frontend запущен и подключается к Backend
- [ ] Админ-панель открывается для главного админа
- [ ] Кнопка "👑 Администраторы" работает
- [ ] Можно добавить нового администратора

---

## 🎉 Готово!

После выполнения всех шагов система мульти-админов должна работать полностью. Вы можете:

1. ✅ Войти как главный админ (ID: 473842863)
2. ✅ Управлять администраторами через интерфейс
3. ✅ Выдавать подписки пользователям
4. ✅ Управлять темами и вопросами

Если возникнут проблемы, проверьте логи backend и консоль браузера для диагностики.
