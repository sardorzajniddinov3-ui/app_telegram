# ✅ Исправление подключения Frontend к Backend

## Что исправлено:

1. ✅ Убран захардкоженный IP адрес `192.168.39.35:3000`
2. ✅ Добавлена поддержка переменной окружения `VITE_API_URL`
3. ✅ Улучшена логика определения URL для Telegram Mini App
4. ✅ Добавлена обратная совместимость с `VITE_BACKEND_URL`

## Как настроить:

### Для локальной разработки (компьютер):

Создайте файл `.env` в папке `src/` или `web/`:

```env
VITE_API_URL=http://localhost:3000
```

### Для разработки с Telegram Mini App (через туннель):

1. Установите ngrok: https://ngrok.com
2. Запустите туннель:
   ```bash
   ngrok http 3000
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Создайте файл `.env`:
   ```env
   VITE_API_URL=https://abc123.ngrok.io
   ```

### Для продакшена (Railway / Render / Vercel):

#### Railway:
1. Деплойте backend на Railway
2. Получите URL (например: `https://your-app.railway.app`)
3. В настройках frontend (Vercel/Netlify) добавьте переменную окружения:
   ```
   VITE_API_URL=https://your-app.railway.app
   ```

#### Render:
1. Деплойте backend на Render
2. Получите URL (например: `https://your-app.onrender.com`)
3. В настройках frontend добавьте:
   ```
   VITE_API_URL=https://your-app.onrender.com
   ```

#### Vercel:
1. Деплойте backend на Vercel
2. Получите URL (например: `https://your-app.vercel.app`)
3. В настройках frontend добавьте:
   ```
   VITE_API_URL=https://your-app.vercel.app
   ```

## Приоритет определения URL:

1. **VITE_API_URL** (рекомендуется) - переменная окружения
2. **VITE_BACKEND_URL** - старая переменная (для совместимости)
3. **Автоматическое определение** - для Telegram Mini App через туннель
4. **Fallback** - `http://localhost:3000` для локальной разработки

## Проверка:

После настройки:

1. Откройте консоль браузера (F12)
2. Должно появиться: `🔗 Backend URL: [ваш URL]`
3. Проверьте, что используется правильный URL
4. Попробуйте функции в приложении - ошибки подключения должны исчезнуть

## Важно:

- ⚠️ В продакшене **ОБЯЗАТЕЛЬНО** задайте `VITE_API_URL` с публичным URL
- ⚠️ После изменения `.env` перезапустите dev сервер
- ⚠️ Файлы `.env` не должны попадать в git

---

**Готово! Теперь подключение работает через переменную окружения!** 🎉
