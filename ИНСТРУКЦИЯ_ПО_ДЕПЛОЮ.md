# 📋 Пошаговая инструкция по деплою Edge Function

## ✅ Что нужно сделать:

### Шаг 1: Проверьте таблицу `ai_explanations` в Supabase

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **SQL Editor**
3. Выполните этот SQL-запрос, чтобы проверить, существует ли таблица:

```sql
SELECT * FROM ai_explanations LIMIT 1;
```

**Если таблица не существует**, выполните SQL из файла `SUPABASE_AI_CACHE_TABLE.sql`:

```sql
-- Создание таблицы для кэширования ответов ИИ
CREATE TABLE IF NOT EXISTS ai_explanations (
  id BIGSERIAL PRIMARY KEY,
  question TEXT NOT NULL,
  wrong_answer TEXT NOT NULL,
  correct_answer TEXT NOT NULL,
  explanation TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(question, wrong_answer, correct_answer)
);

-- Включить RLS (Row Level Security)
ALTER TABLE ai_explanations ENABLE ROW LEVEL SECURITY;

-- Политики для чтения (все могут читать)
CREATE POLICY "Allow public read access" ON ai_explanations
  FOR SELECT USING (true);

-- Политики для записи (все могут писать)
CREATE POLICY "Allow public insert access" ON ai_explanations
  FOR INSERT WITH CHECK (true);

-- Политики для обновления (все могут обновлять)
CREATE POLICY "Allow public update access" ON ai_explanations
  FOR UPDATE USING (true);
```

---

### Шаг 2: Задеплойте Edge Function

#### Вариант A: Через веб-интерфейс (РЕКОМЕНДУЕТСЯ)

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **Edge Functions** (в левом меню)
3. Найдите функцию `explain-answer`:
   - Если функция уже существует → нажмите на неё для редактирования
   - Если функции нет → нажмите **Create a new function** → название: `explain-answer`
4. Скопируйте **весь код** из файла `supabase/functions/explain-answer/index.ts`
5. Вставьте код в редактор функции
6. Нажмите **Deploy** или **Save and Deploy**

#### Вариант B: Через командную строку

```bash
# Перейдите в папку проекта
cd apptg111/apptg/telegram-mini-app

# Задеплойте функцию
supabase functions deploy explain-answer
```

---

### Шаг 3: Проверьте переменные окружения

1. В **Supabase Dashboard** перейдите в **Project Settings** (⚙️)
2. Найдите раздел **Edge Functions** → **Secrets**
3. Убедитесь, что есть следующие секреты:

#### Обязательные секреты:

- **`GEMINI_API_KEY`**
  - Значение: `AIzaSyCB4BeQ6l5nf19eHLbSD4NeIZyiH8RExOk`
  - Для получения нового ключа: https://aistudio.google.com/app/apikey

#### Автоматически доступные (не нужно добавлять вручную):

- **`SUPABASE_URL`** - автоматически доступен в Edge Functions
- **`SUPABASE_ANON_KEY`** - автоматически доступен в Edge Functions
- **`SUPABASE_SERVICE_ROLE_KEY`** - автоматически доступен в Edge Functions (рекомендуется для записи)

**Если секрета `GEMINI_API_KEY` нет:**
1. Нажмите **Add new secret** или **New Secret**
2. **Name**: `GEMINI_API_KEY`
3. **Value**: `AIzaSyCB4BeQ6l5nf19eHLbSD4NeIZyiH8RExOk`
4. Нажмите **Save**

---

### Шаг 4: Проверьте работу функции

1. После деплоя откройте **Edge Functions** → `explain-answer` → **Logs**
2. В приложении ответьте неправильно на вопрос в тесте
3. Нажмите кнопку **"🤖 Почему это неправильно?"**
4. Проверьте логи в Supabase Dashboard:
   - Должны появиться логи с эмодзи: `🔄`, `✅`, `📤`, `💾`
   - Если есть ошибки, они будут с эмодзи: `❌`, `⚠️`

---

### Шаг 5: Проверьте сохранение в базу

1. Откройте **Supabase Dashboard** → **Table Editor**
2. Найдите таблицу `ai_explanations`
3. Проверьте, что там появились записи после запросов к ИИ

---

## 🔍 Отладка проблем

### Проблема: "Ошибка сохранения в кэш: PGRST204"

**Решение:** Это нормально! Код автоматически сохранит данные без колонки `question_embedding`. Если хотите добавить семантический поиск, выполните SQL из файла `SUPABASE_AI_CACHE_EMBEDDINGS.sql`.

### Проблема: "Ошибка: Нет ключа API"

**Решение:** Убедитесь, что секрет `GEMINI_API_KEY` добавлен в **Project Settings** → **Edge Functions** → **Secrets**.

### Проблема: "Ошибка RLS (42501)"

**Решение:** Убедитесь, что RLS политики для таблицы `ai_explanations` разрешают INSERT/UPDATE (см. SQL выше).

### Проблема: Функция не вызывается

**Решение:** 
1. Проверьте URL функции в коде фронтенда
2. Убедитесь, что функция задеплоена
3. Проверьте CORS настройки (должны быть настроены в коде функции)

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ Edge Function задеплоена
- ✅ Переменные окружения настроены
- ✅ Таблица `ai_explanations` создана
- ✅ Ответы ИИ сохраняются в базу данных
- ✅ Автоматическое переключение между моделями работает

Теперь ваше приложение готово к работе! 🎉
