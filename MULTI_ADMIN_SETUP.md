# Настройка мульти-админ системы

## Что было сделано

1. ✅ Создана таблица `admins` в PostgreSQL для хранения списка администраторов
2. ✅ Обновлен backend для поддержки нескольких администраторов
3. ✅ Добавлены API endpoints для управления администраторами
4. ✅ Обновлен frontend для проверки админ-доступа через API
5. ✅ Добавлен интерфейс управления администраторами в админ-панель
6. ✅ Улучшена обработка ошибок (404 и другие)

## Применение миграции БД

### Вариант 1: Автоматическая миграция (рекомендуется)

**В PowerShell:**

1. Откройте PowerShell в корне проекта `telegram-mini-app`
2. Перейдите в директорию backend:
   ```powershell
   cd backend
   ```
3. Запустите миграцию:
   ```powershell
   npm run migrate
   ```

**Если возникают проблемы с кодировкой пути:**

Используйте короткий путь или запустите напрямую:
```powershell
# Из корня проекта telegram-mini-app
Set-Location backend
npm run migrate
```

### Вариант 2: Ручное применение SQL (рекомендуется, если миграция не работает)

Если у вас проблемы с кодировкой пути в PowerShell, выполните SQL напрямую в PostgreSQL:

1. Откройте ваш клиент PostgreSQL (pgAdmin, DBeaver, psql и т.д.)
2. Подключитесь к вашей базе данных
3. Выполните SQL из файла `backend/ADMINS_MIGRATION.sql`

Или скопируйте и выполните этот SQL:

```sql
-- Admins table
CREATE TABLE IF NOT EXISTS admins (
  id SERIAL PRIMARY KEY,
  telegram_id BIGINT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  created_by BIGINT,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Insert main admin (473842863) if not exists
INSERT INTO admins (telegram_id, created_by)
SELECT 473842863, 473842863
WHERE NOT EXISTS (SELECT 1 FROM admins WHERE telegram_id = 473842863);

-- Index
CREATE INDEX IF NOT EXISTS idx_admins_telegram_id ON admins(telegram_id);
```

**Проверка:** После выполнения выполните `SELECT * FROM admins;` чтобы убедиться, что главный админ добавлен.

**Важно:** Убедитесь, что в файле `.env` в папке `backend` указан правильный `DATABASE_URL`:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/database_name
```

Важно: Таблица `admins` будет создана автоматически, и главный админ (ID: 473842863) будет добавлен автоматически.

## Новые API endpoints

### Проверка админ-доступа
```
GET /api/admin/check
```
Возвращает `{ isAdmin: true/false }`

### Получить список администраторов
```
GET /api/admin/admins
```
Требует админ-доступ. Возвращает `{ admins: [...] }`

### Добавить администратора
```
POST /api/admin/admins
Body: { telegramId: number }
```
Требует админ-доступ. Добавляет нового администратора.

### Удалить администратора
```
DELETE /api/admin/admins/:telegramId
```
Требует админ-доступ. Удаляет администратора (нельзя удалить главного админа или себя).

## Использование

1. **Главный админ (ID: 473842863)** всегда имеет доступ и не может быть удален
2. **Другие администраторы** могут быть добавлены через интерфейс админ-панели
3. **Проверка админ-доступа** теперь происходит через API, а не через хардкод

## Интерфейс управления администраторами

В админ-панели появилась новая кнопка **"👑 Администраторы"**, которая открывает экран управления администраторами.

На этом экране можно:
- Просмотреть список всех администраторов
- Добавить нового администратора по Telegram ID
- Удалить администратора (кроме главного и себя)

## Устранение ошибок 404

Улучшена обработка ошибок:
- Более понятные сообщения об ошибках
- Проверка подключения к backend
- Корректная обработка сетевых ошибок

Если видите ошибку 404:
1. Убедитесь, что backend запущен (`npm start` в папке `backend`)
2. Проверьте, что `VITE_BACKEND_URL` в `.env` файле указывает на правильный адрес
3. Проверьте, что PostgreSQL запущен и доступен

## Безопасность

- Все админские роуты защищены middleware `adminOnly`
- Проверка админ-доступа происходит через БД
- Главный админ (473842863) всегда имеет доступ
- Нельзя удалить главного админа или себя
