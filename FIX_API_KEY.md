# 🔧 Исправление ошибки "Invalid API key"

## ❌ Проблема

Ошибка `401 Unauthorized` или `Invalid API key` означает, что API ключ в файле `.env` неверный или от другого проекта.

## ✅ Решение

### Шаг 1: Получите правильный ключ из Supabase Dashboard

1. Откройте https://supabase.com/dashboard
2. **ВАЖНО:** Выберите проект **`psjtbcotmnfvgulziara`** (НЕ `memoqljluizvccomaind`!)
3. Перейдите: **Settings** → **API**
4. Найдите секцию **Project API keys**
5. Скопируйте **anon/public** ключ (это длинный JWT токен)

### Шаг 2: Обновите файл `.env`

Откройте файл `.env` в корне проекта `telegram-mini-app` и обновите:

```env
VITE_SUPABASE_URL=https://psjtbcotmnfvgulziara.supabase.co
VITE_SUPABASE_ANON_KEY=<вставьте_скопированный_ключ_здесь>
```

**Важно:** 
- Ключ должен быть от проекта `psjtbcotmnfvgulziara`
- Если в JWT токене указан другой ref (например, `memoqljluizvccomaind`) - это неправильный ключ!

### Шаг 3: Перезапустите dev-сервер

```powershell
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

### Шаг 4: Проверьте консоль браузера

Откройте консоль браузера (F12) и проверьте сообщения:

- ✅ `✅ [SUPABASE] API ключ соответствует проекту psjtbcotmnfvgulziara` - ключ правильный
- ✅ `✅ [SUPABASE] Подключение к базе данных успешно` - всё работает
- ❌ `❌ [SUPABASE] КРИТИЧЕСКАЯ ОШИБКА: API ключ от другого проекта!` - ключ неверный

## 🔍 Как проверить, что ключ правильный?

1. Откройте консоль браузера (F12)
2. Найдите сообщение с проверкой ключа
3. Если видите `❌ КРИТИЧЕСКАЯ ОШИБКА: API ключ от другого проекта!` - ключ неверный
4. Если видите `✅ API ключ соответствует проекту psjtbcotmnfvgulziara` - ключ правильный

## ⚠️ Частые ошибки

1. **Использование ключа от старого проекта** (`memoqljluizvccomaind`)
   - Решение: Получите ключ от проекта `psjtbcotmnfvgulziara`

2. **Неправильный формат ключа**
   - Ключ должен быть длинным JWT токеном (начинается с `eyJhbGci...`)
   - Проверьте, что скопировали весь ключ без пробелов

3. **Файл .env не перезагружен**
   - После изменения `.env` обязательно перезапустите dev-сервер

## 📝 Пример правильного .env файла

```env
VITE_SUPABASE_URL=https://psjtbcotmnfvgulziara.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBzanRiY290bW5mdmd1bHppYXJhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4ODYwODEsImV4cCI6MjA4NjQ2MjA4MX0.ваша_подпись_здесь
VITE_BACKEND_URL=http://localhost:3000
```

**Важно:** Замените `ваша_подпись_здесь` на реальный ключ из Supabase Dashboard!
