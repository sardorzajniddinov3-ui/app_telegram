# 🚀 Деплой Edge Function через Dashboard (РЕКОМЕНДУЕТСЯ)

## ✅ Самый простой способ - без установки CLI!

---

## 📋 Пошаговая инструкция:

### Шаг 1: Откройте Supabase Dashboard
1. Зайдите на https://supabase.com/dashboard
2. Выберите ваш проект

### Шаг 2: Создайте новую Edge Function
1. В левом меню найдите **Edge Functions** (или **Functions**)
2. Нажмите кнопку **Create a new function** (или **New Function**)
3. Введите название: `ai-trainer-advice`
4. Нажмите **Create function**

### Шаг 3: Скопируйте код функции
1. Откройте файл в вашем проекте:
   ```
   supabase/functions/ai-trainer-advice/index.ts
   ```
2. **Скопируйте ВЕСЬ код** из этого файла (Ctrl+A, Ctrl+C)
3. В Dashboard в редакторе функции **вставьте код** (Ctrl+V)
4. Нажмите кнопку **Deploy** (или **Save & Deploy**)

### Шаг 4: Добавьте секрет (API ключ Gemini)
1. В функции `ai-trainer-advice` откройте вкладку **Settings** (или **Configuration**)
2. Найдите раздел **Secrets** (или **Environment Variables**)
3. Нажмите **Add new secret** (или **+ Add Secret**)
4. Заполните:
   - **Name:** `GEMINI_API_KEY`
   - **Value:** ваш ключ Google Gemini API (получить можно на https://aistudio.google.com/apikey)
5. Нажмите **Save**

### Шаг 5: Проверка работы
1. Откройте вкладку **Logs** в функции
2. Должны быть логи без ошибок
3. Можно протестировать через вкладку **Invoke** (см. ниже)

---

## 🧪 Тестирование функции

### Где найти вкладку "Invoke":

1. В Supabase Dashboard откройте **Edge Functions** (левое меню)
2. Найдите функцию **ai-trainer-advice** в списке
3. Нажмите на название функции, чтобы открыть её
4. Вверху будут вкладки: **Code**, **Logs**, **Invoke**, **Settings**
5. Откройте вкладку **Invoke**

### Тестирование через Dashboard (вкладка Invoke):

Вставьте тестовый JSON в поле:

```json
{
  "userId": 123456789,
  "errors": [
    {
      "question": "Что означает знак 'STOP'?",
      "wrong": "Уступи дорогу",
      "correct": "Остановка обязательна"
    }
  ],
  "correctCount": 15,
  "totalCount": 20
}
```

Нажмите **Invoke** - должен вернуться JSON с полем `advice`.

---

## ✅ Готово!

Функция задеплоена и готова к использованию!

Теперь продолжайте с **Шага 3** из `AI_TRAINER_IMPLEMENTATION_GUIDE.md` (интеграция в код приложения).

---

## ⚠️ Если возникли проблемы:

### Ошибка: "Function not found"
- Убедитесь, что функция называется точно `ai-trainer-advice`
- Проверьте, что функция задеплоена (статус должен быть "Active")

### Ошибка: "GEMINI_API_KEY not found"
- Проверьте, что секрет добавлен в Settings → Secrets
- Убедитесь, что название секрета точно `GEMINI_API_KEY` (без пробелов)

### Ошибка: "429 Rate Limit"
- Это нормально - система автоматически переключится на другую модель
- Проверьте логи в Dashboard

---

## 📝 Альтернатива: Установка CLI (если нужен)

Если всё же хотите использовать CLI, установите через **Scoop**:

```powershell
# Установка Scoop (если не установлен)
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
irm get.scoop.sh | iex

# Установка Supabase CLI
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

Но для деплоя одной функции Dashboard проще! 🎯
