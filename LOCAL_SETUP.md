# 🚀 Инструкция по локальному запуску

## Требования

- **Node.js** 18+ (проверьте: `node --version`)
- **PostgreSQL** (установлен и запущен)
- **npm** или **yarn**

---

## 📋 Шаг 1: Настройка базы данных PostgreSQL

1. Установите PostgreSQL, если еще не установлен
2. Создайте базу данных:
   ```sql
   CREATE DATABASE telegram_mini_app;
   ```
3. Запомните данные для подключения:
   - Хост: `localhost`
   - Порт: `5432` (по умолчанию)
   - Имя базы: `telegram_mini_app`
   - Пользователь: `postgres` (или ваш)
   - Пароль: ваш пароль

---

## 🔧 Шаг 2: Настройка Backend

1. Перейдите в папку backend:
   ```bash
   cd backend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` (скопируйте из `env.example`):
   ```bash
   # Windows PowerShell
   Copy-Item env.example .env
   
   # Linux/Mac
   cp env.example .env
   ```

4. Отредактируйте `.env` файл:
   ```env
   PORT=3000
   DATABASE_URL=postgres://postgres:ВАШ_ПАРОЛЬ@localhost:5432/telegram_mini_app
   RUN_MIGRATIONS=true
   ```
   
   ⚠️ **Замените `ВАШ_ПАРОЛЬ` на ваш пароль PostgreSQL!**

5. Примените миграции (создаст таблицы в БД):
   ```bash
   npm run migrate
   ```

6. Запустите backend сервер:
   ```bash
   # Обычный запуск
   npm start
   
   # Или с автоперезагрузкой (для разработки)
   npm run dev
   ```

   ✅ Backend должен запуститься на `http://localhost:3000`

---

## 🎨 Шаг 3: Настройка Frontend

1. Откройте **новый терминал** и перейдите в папку web:
   ```bash
   cd web
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. (Опционально) Создайте файл `.env.local` для настройки URL backend:
   ```env
   VITE_BACKEND_URL=http://localhost:3000
   ```
   
   ⚠️ Если не создадите, будет использован `http://localhost:3000` по умолчанию.

4. Запустите frontend сервер разработки:
   ```bash
   npm run dev
   ```

   ✅ Frontend должен запуститься на `http://localhost:5173` (или другой порт, смотрите в консоли)

---

## ✅ Проверка работы

1. **Backend**: Откройте в браузере `http://localhost:3000/health`
   - Должен вернуться: `{"ok":true}`

2. **Frontend**: Откройте URL, который показал Vite (обычно `http://localhost:5173`)
   - Должен открыться интерфейс приложения

---

## 🐛 Решение проблем

### Backend не запускается

- **Ошибка подключения к БД**: Проверьте `DATABASE_URL` в `.env`, убедитесь что PostgreSQL запущен
- **Порт занят**: Измените `PORT` в `.env` на другой (например, `3001`)

### Frontend не подключается к Backend

- Проверьте, что backend запущен на `http://localhost:3000`
- Проверьте переменную `VITE_BACKEND_URL` в `.env.local`
- Откройте консоль браузера (F12) и проверьте ошибки сети

### Миграции не применяются

- Убедитесь, что база данных создана
- Проверьте права пользователя PostgreSQL
- Запустите миграции вручную: `npm run migrate`

---

## 📝 Быстрый старт (краткая версия)

```bash
# Терминал 1: Backend
cd backend
npm install
cp env.example .env
# Отредактируйте .env (укажите пароль PostgreSQL)
npm run migrate
npm start

# Терминал 2: Frontend
cd web
npm install
npm run dev
```

---

## 🔐 Админ-доступ

- Telegram ID для админ-панели: **473842863**
- Админ-панель доступна только этому пользователю

---

## 📚 Полезные команды

### Backend
- `npm start` - запуск сервера
- `npm run dev` - запуск с автоперезагрузкой
- `npm run migrate` - применить миграции
- `npm test` - запустить тесты

### Frontend
- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр собранного приложения
