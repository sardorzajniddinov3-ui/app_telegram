# 📋 Пошаговая инструкция - Настройка системы

## ✅ Шаг 1: Таблица admins (УЖЕ ГОТОВО)
Таблица создана в Supabase. Продолжаем дальше.

---

## 📝 Шаг 2: Создать файл backend/.env

### Вариант A: Через файловый менеджер
1. Откройте папку `backend`
2. Создайте новый файл с именем `.env` (без расширения)
3. Скопируйте содержимое из `backend/.env.template`
4. Замените `[YOUR-PASSWORD]` и `[YOUR-PROJECT-REF]` на реальные значения

### Вариант B: Через PowerShell
```powershell
cd "apptg\telegram-mini-app\backend"
Copy-Item "env.example" ".env"
# Затем откройте .env и отредактируйте DATABASE_URL
```

### Как получить DATABASE_URL:
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите: **Settings** → **Database**
4. Найдите **Connection string**
5. Выберите вкладку **URI**
6. Скопируйте строку (она выглядит так):
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
   ```
7. Вставьте в файл `.env` вместо `DATABASE_URL=...`

**Пример готового backend/.env:**
```env
PORT=3000
DATABASE_URL=postgresql://postgres.abcdefghijklmnop:MyPassword123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
RUN_MIGRATIONS=false
```

---

## 📝 Шаг 3: Создать файл .env в корне проекта

### Вариант A: Через файловый менеджер
1. Откройте папку `telegram-mini-app` (корень проекта)
2. Создайте новый файл с именем `.env`
3. Добавьте строку:
   ```
   VITE_BACKEND_URL=http://localhost:3000
   ```

### Вариант B: Через PowerShell
```powershell
cd "apptg\telegram-mini-app"
@"
VITE_BACKEND_URL=http://localhost:3000
"@ | Out-File -FilePath ".env" -Encoding utf8
```

---

## 🚀 Шаг 4: Запустить Backend

Откройте PowerShell в папке `backend`:

```powershell
cd "apptg\telegram-mini-app\backend"
npm install
npm start
```

**Ожидаемый результат:**
```
Server running on port 3000
```

Если видите ошибки подключения к базе данных:
- Проверьте `DATABASE_URL` в `backend/.env`
- Убедитесь, что пароль правильный
- Проверьте, что Supabase проект активен

---

## 🎨 Шаг 5: Запустить Frontend

Откройте **новое окно PowerShell** в папке проекта:

```powershell
cd "apptg\telegram-mini-app"
npm install
npm run dev
```

**Ожидаемый результат:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## ✅ Шаг 6: Проверить работу

1. **Откройте приложение в Telegram**
   - Должно открыться ваше мини-приложение

2. **Войдите как главный админ**
   - Telegram ID: `473842863`
   - Должна открыться админ-панель

3. **Проверьте раздел "Администраторы"**
   - Нажмите кнопку **"👑 Администраторы"**
   - Должен отобразиться список с главным админом

4. **Попробуйте добавить администратора**
   - Введите Telegram ID другого пользователя
   - Нажмите "Добавить администратора"
   - Должен появиться новый админ в списке

---

## ❗ Если что-то не работает:

### Backend не запускается
- ✅ Проверьте, что файл `backend/.env` существует
- ✅ Проверьте, что `DATABASE_URL` правильный
- ✅ Убедитесь, что порт 3000 свободен
- ✅ Проверьте логи на наличие ошибок

### Ошибка подключения к базе данных
- ✅ Проверьте пароль в `DATABASE_URL`
- ✅ Убедитесь, что используете формат URI
- ✅ Проверьте, что Supabase проект активен
- ✅ Попробуйте пересоздать Connection String в Supabase

### Frontend не видит backend
- ✅ Проверьте, что backend запущен
- ✅ Проверьте `VITE_BACKEND_URL` в `.env`
- ✅ Убедитесь, что оба сервера запущены
- ✅ Проверьте консоль браузера на ошибки

### Админ-панель не открывается
- ✅ Проверьте, что вы вошли как правильный пользователь (ID: 473842863)
- ✅ Проверьте, что таблица `admins` в Supabase содержит этого пользователя:
  ```sql
  SELECT * FROM admins WHERE telegram_id = 473842863;
  ```
- ✅ Проверьте логи backend
- ✅ Проверьте консоль браузера

---

## 📞 Нужна помощь?

Если что-то не получается:
1. Проверьте логи backend (в консоли где запущен `npm start`)
2. Проверьте консоль браузера (F12 → Console)
3. Убедитесь, что все файлы `.env` созданы и заполнены правильно
4. Проверьте, что Supabase проект активен и доступен

---

## 🎉 Готово!

После выполнения всех шагов система должна работать полностью!
