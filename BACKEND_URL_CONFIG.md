# 🔧 Настройка Backend URL

## Проблема решена!

Теперь backend URL настраивается через переменную окружения `VITE_API_URL`, без захардкоженных IP адресов.

## Настройка для разных окружений

### 1. Локальная разработка (компьютер)

Создайте файл `.env` в папке `src` или `web`:

```env
VITE_API_URL=http://localhost:3000
```

### 2. Разработка с Telegram Mini App (через туннель)

Используйте ngrok, localtunnel или другой туннель:

```bash
# Установите ngrok
ngrok http 3000
```

Создайте файл `.env`:

```env
VITE_API_URL=https://your-tunnel-id.ngrok.io
```

### 3. Продакшен (Railway / Render / Vercel)

#### Railway:
```env
VITE_API_URL=https://your-app.railway.app
```

#### Render:
```env
VITE_API_URL=https://your-app.onrender.com
```

#### Vercel:
```env
VITE_API_URL=https://your-app.vercel.app
```

## Как это работает

1. **Приоритет 1:** Переменная `VITE_API_URL` (рекомендуется)
2. **Приоритет 2:** Переменная `VITE_BACKEND_URL` (для обратной совместимости)
3. **Приоритет 3:** Автоматическое определение для Telegram Mini App
4. **Приоритет 4:** Fallback на `http://localhost:3000` для локальной разработки

## Для Telegram Mini App

В продакшене **обязательно** задайте `VITE_API_URL` с публичным URL вашего backend:

```env
VITE_API_URL=https://your-backend.railway.app
```

## Проверка

После настройки проверьте в консоли браузера (F12):
- Должен использоваться правильный URL
- Не должно быть ошибок подключения

---

**Важно:** После изменения `.env` файла перезапустите dev сервер!
