# 🔧 Исправление проблемы с отображением изображений

## ❌ Проблема

- **Старый формат (base64):** `data:image/png;base64,iVBORw0KGgo...` - **работает** ✅
- **Новый формат (Supabase Storage):** пути к файлам в Storage - **не работает** ❌

## 🔍 Диагностика

### 1. Откройте консоль браузера (F12)

При загрузке изображений вы увидите логи:

**Для base64 изображений:**
```
✅ [IMAGE] Base64 изображение: data:image/png;base64,iVBORw0KGgo...
```

**Для полных URL:**
```
✅ [IMAGE] Полный URL (возвращаем как есть): https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/question-images/questions/123.webp
```

**Для путей к Storage:**
```
🔍 [IMAGE] Обработка пути к Storage: questions/123.webp
🔍 [IMAGE] Формирование URL через getPublicUrl: { bucketName: 'question-images', filePath: 'questions/123.webp' }
✅ [IMAGE] URL сформирован через getPublicUrl: { ... }
```

**Если URL от старого проекта:**
```
⚠️ [IMAGE] Обнаружен URL от старого проекта: memoqljluizvccomaind
⚠️ [IMAGE] Старый URL: https://memoqljluizvccomaind.supabase.co/...
✅ [IMAGE] Обновлен URL на новый проект: https://psjtbcotmnfvgulziara.supabase.co/...
```

### 2. Проверьте, что сохранено в базе данных

Выполните SQL запрос в Supabase SQL Editor:

```sql
-- Проверьте формат путей к изображениям
SELECT 
  id,
  question_text,
  image_url,
  CASE 
    WHEN image_url LIKE 'data:image/%' THEN 'Base64 (старый формат)'
    WHEN image_url LIKE 'https://%' THEN 'Полный URL (новый формат)'
    WHEN image_url LIKE 'question-images/%' THEN 'Путь с bucket name'
    WHEN image_url LIKE 'questions/%' THEN 'Путь без bucket name'
    WHEN image_url IS NULL OR image_url = '' THEN 'Нет изображения'
    ELSE 'Неизвестный формат'
  END as image_format,
  LENGTH(image_url) as url_length
FROM questions
WHERE image_url IS NOT NULL AND image_url != ''
ORDER BY id DESC
LIMIT 20;
```

### 3. Проверьте, какие URL от старого проекта

```sql
-- Найдите все URL от старых проектов
SELECT 
  id,
  question_text,
  image_url
FROM questions
WHERE image_url LIKE '%rjfchznkmulatifulele.supabase.co%'
   OR image_url LIKE '%memoqljluizvccomaind.supabase.co%'
LIMIT 20;
```

## ✅ Решения

### Решение 1: Обновить URL от старых проектов в базе данных

Если в базе есть URL от старых проектов, выполните SQL запрос:

```sql
-- Обновить все URL от старых проектов на новый
UPDATE questions
SET image_url = REPLACE(
  REPLACE(
    image_url,
    'https://rjfchznkmulatifulele.supabase.co',
    'https://psjtbcotmnfvgulziara.supabase.co'
  ),
  'https://memoqljluizvccomaind.supabase.co',
  'https://psjtbcotmnfvgulziara.supabase.co'
)
WHERE image_url LIKE '%rjfchznkmulatifulele.supabase.co%'
   OR image_url LIKE '%memoqljluizvccomaind.supabase.co%';
```

**Примечание:** Код теперь автоматически обновляет старые URL, но лучше обновить их в базе данных.

### Решение 2: Проверить, что файлы существуют в Storage

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите проект **psjtbcotmnfvgulziara**
3. Перейдите: **Storage** → **question-images** → **questions/**
4. Проверьте, что файлы существуют

Если файла нет:
- Файл был удален
- Файл не был загружен
- Путь в базе данных неверный

### Решение 3: Проверить формат пути в базе данных

**Правильные форматы:**

1. **Полный URL (рекомендуется):**
   ```
   https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/question-images/questions/123.webp
   ```

2. **Путь с bucket name:**
   ```
   question-images/questions/123.webp
   ```

3. **Путь без bucket name:**
   ```
   questions/123.webp
   ```

4. **Base64 (старый формат, работает, но не рекомендуется):**
   ```
   data:image/png;base64,iVBORw0KGgo...
   ```

**Неправильные форматы:**

- URL от старого проекта (автоматически исправляется в коде)
- Неполные пути без указания bucket или папки

### Решение 4: Если файл не существует в Storage

Если в базе есть путь к файлу, но файл не существует в Storage:

**Вариант 1:** Удалить запись из базы данных (если изображение не нужно)
```sql
UPDATE questions
SET image_url = NULL
WHERE image_url = 'https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/question-images/questions/123.webp';
```

**Вариант 2:** Загрузить файл вручную в Storage
1. Откройте **Storage** → **question-images** → **questions/**
2. Нажмите **Upload file**
3. Загрузите файл с правильным именем

**Вариант 3:** Обновить путь в базе данных на существующий файл

## 🔍 Что делает код сейчас

1. **Base64 изображения** - возвращаются как есть ✅
2. **Полные URL** - проверяются на старые проекты и автоматически обновляются ✅
3. **Пути к Storage** - автоматически преобразуются в полные URL ✅
4. **Ошибки загрузки** - изображения автоматически скрываются при ошибке 404 ✅

## 📝 Проверка после исправления

1. Откройте консоль браузера (F12)
2. Перезагрузите страницу
3. Проверьте логи:
   - Должны быть сообщения `✅ [IMAGE]` для успешных изображений
   - Не должно быть ошибок `❌ [IMAGE]`
   - При ошибке 404 должно быть предупреждение `⚠️ [IMAGE] Ошибка загрузки изображения`

4. Проверьте отображение:
   - Base64 изображения должны отображаться
   - Изображения из Storage должны отображаться
   - Если изображение не найдено (404), оно должно скрываться без ошибок

## ⚠️ Важно

- Код автоматически обрабатывает все форматы
- Старые URL проектов автоматически обновляются
- Ошибки загрузки обрабатываются автоматически
- Но лучше обновить URL в базе данных для производительности
