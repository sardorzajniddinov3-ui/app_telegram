# 🚀 Настройка переменных окружения в Vercel

## ❌ Проблема

Ошибка `401 Unauthorized` на Vercel означает, что переменные окружения не настроены в Vercel Dashboard.

**Локально работает** → потому что используется файл `.env`  
**На Vercel не работает** → потому что `.env` не загружается автоматически

## ✅ Решение: Настройка переменных окружения в Vercel

### Шаг 1: Откройте Vercel Dashboard

1. Перейдите на https://vercel.com/dashboard
2. Войдите в свой аккаунт
3. Найдите ваш проект (например, `sardorzajniddinov3-ui-apptelegram`)

### Шаг 2: Перейдите в настройки проекта

1. Нажмите на название проекта
2. В верхнем меню выберите **Settings**
3. В левом меню выберите **Environment Variables**

### Шаг 3: Добавьте переменные окружения

Нажмите **Add New** и добавьте следующие переменные:

#### Переменная 1: `VITE_SUPABASE_URL`

- **Name:** `VITE_SUPABASE_URL`
- **Value:** `https://psjtbcotmnfvgulziara.supabase.co`
- **Environment:** Выберите все окружения (Production, Preview, Development)

#### Переменная 2: `VITE_SUPABASE_ANON_KEY`

- **Name:** `VITE_SUPABASE_ANON_KEY`
- **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBzanRiY290bW5mdmd1bHppYXJhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4ODYzMzcsImV4cCI6MjA4NjQ2MjMzN30.AXmMzg9GasqUzIUBXdSPPvsZlsMIuMSRXvGTcqQNXKQ`
- **Environment:** Выберите все окружения (Production, Preview, Development)

#### Переменная 3: `VITE_BACKEND_URL` (опционально)

- **Name:** `VITE_BACKEND_URL`
- **Value:** `https://your-backend-url.railway.app` (или ваш backend URL)
- **Environment:** Выберите все окружения

### Шаг 4: Сохраните и передеплойте

1. Нажмите **Save** для каждой переменной
2. Перейдите в раздел **Deployments**
3. Найдите последний деплой и нажмите **Redeploy** (или создайте новый деплой через Git push)

## 📸 Визуальная инструкция

### Где найти Environment Variables:

```
Vercel Dashboard
  → Ваш проект
    → Settings (верхнее меню)
      → Environment Variables (левое меню)
```

### Как добавить переменную:

1. Нажмите **Add New**
2. Введите **Name:** `VITE_SUPABASE_URL`
3. Введите **Value:** `https://psjtbcotmnfvgulziara.supabase.co`
4. Выберите окружения: ☑️ Production ☑️ Preview ☑️ Development
5. Нажмите **Save**

Повторите для `VITE_SUPABASE_ANON_KEY` с правильным значением.

## ⚠️ Важно

1. **Префикс `VITE_` обязателен!** Vite использует только переменные с префиксом `VITE_`
2. **После добавления переменных нужно передеплоить проект!**
3. **Не добавляйте `.env` файл в Git!** Он должен быть в `.gitignore`

## 🔍 Проверка

После передеплоя:

1. Откройте ваше приложение на Vercel
2. Откройте консоль браузера (F12)
3. Проверьте сообщения:
   - ✅ `✅ [SUPABASE] API ключ соответствует проекту psjtbcotmnfvgulziara`
   - ✅ `✅ [SUPABASE] Подключение к базе данных успешно`
   - ❌ Ошибки `401 Unauthorized` должны исчезнуть

## 🚨 Если ошибка осталась

1. **Проверьте, что переменные добавлены правильно:**
   - Имя: `VITE_SUPABASE_URL` (с префиксом `VITE_`)
   - Значение: `https://psjtbcotmnfvgulziara.supabase.co` (без пробелов)

2. **Убедитесь, что проект передеплоен:**
   - Перейдите в **Deployments**
   - Создайте новый деплой или нажмите **Redeploy**

3. **Проверьте логи деплоя:**
   - В разделе **Deployments** откройте последний деплой
   - Проверьте **Build Logs** на наличие ошибок

4. **Проверьте консоль браузера:**
   - Откройте приложение на Vercel
   - Нажмите F12 → Console
   - Проверьте сообщения о переменных окружения

## 📝 Пример правильных переменных

```
VITE_SUPABASE_URL=https://psjtbcotmnfvgulziara.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBzanRiY290bW5mdmd1bHppYXJhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4ODYzMzcsImV4cCI6MjA4NjQ2MjMzN30.AXmMzg9GasqUzIUBXdSPPvsZlsMIuMSRXvGTcqQNXKQ
VITE_BACKEND_URL=https://your-backend.railway.app
```

## ✅ После настройки

После добавления переменных и передеплоя:
- ✅ Ошибка `401 Unauthorized` исчезнет
- ✅ Приложение будет работать на Vercel
- ✅ Подключение к Supabase будет успешным
