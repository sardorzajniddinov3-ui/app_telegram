# Быстрая миграция таблицы admins

## Проблема с PowerShell и кириллицей в пути

Если у вас проблемы с запуском `npm run migrate` из-за кириллицы в пути (`Новая папка`), выполните SQL напрямую в PostgreSQL.

## Инструкция

### Шаг 1: Откройте ваш клиент PostgreSQL

Используйте любой из следующих инструментов:
- **pgAdmin** (графический интерфейс)
- **DBeaver** (универсальный клиент БД)
- **psql** (командная строка)
- **VS Code** с расширением PostgreSQL
- Любой другой SQL-клиент

### Шаг 2: Подключитесь к вашей базе данных

Убедитесь, что подключены к той же базе данных, которую использует ваш backend.

### Шаг 3: Выполните SQL

Скопируйте и выполните следующий SQL:

```sql
-- Создание таблицы admins
CREATE TABLE IF NOT EXISTS admins (
  id SERIAL PRIMARY KEY,
  telegram_id BIGINT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  created_by BIGINT,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Добавление главного админа (473842863)
INSERT INTO admins (telegram_id, created_by)
SELECT 473842863, 473842863
WHERE NOT EXISTS (SELECT 1 FROM admins WHERE telegram_id = 473842863);

-- Создание индекса для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_admins_telegram_id ON admins(telegram_id);
```

### Шаг 4: Проверка

Выполните запрос для проверки:

```sql
SELECT * FROM admins;
```

Должна быть одна запись:
- `telegram_id = 473842863`
- `created_by = 473842863`

## Готово! ✅

После выполнения SQL миграция завершена. Теперь можно:

1. Перезапустить backend (если он был запущен)
2. Войти в админ-панель как главный админ (ID: 473842863)
3. Добавить других администраторов через интерфейс "👑 Администраторы"

## Альтернатива: Использовать файл

Если хотите, можете открыть файл `backend/ADMINS_MIGRATION.sql` в вашем SQL-клиенте и выполнить его.
