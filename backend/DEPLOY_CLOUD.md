# ☁️ Деплой бэкенда в облако (работа 24/7)

## ⚠️ Важно понимать

**Локальный бэкенд работает только когда ваш ПК включен!**

Если ПК выключен → бэкенд не работает → приложение не работает.

## ✅ Решение: Деплой в облако

Для работы 24/7 нужно развернуть бэкенд на облачном сервере.

## 🚀 Вариант 1: Railway (Рекомендуется - самый простой)

### Преимущества:
- ✅ Бесплатный тариф (500 часов/месяц)
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ Автоматический HTTPS

### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Выберите папку `backend`

3. **Настройте переменные окружения:**
   - В настройках проекта → Variables
   - Добавьте:
     ```
     PORT=3000
     DATABASE_URL=postgresql://... (ваш Supabase URL)
     RUN_MIGRATIONS=true
     ```

4. **Railway автоматически:**
   - Установит зависимости (`npm install`)
   - Запустит бэкенд (`npm start`)
   - Выдаст публичный URL (например: `https://your-app.railway.app`)

5. **Используйте URL в frontend:**
   - В настройках frontend (Vercel/Netlify) добавьте:
     ```
     VITE_API_URL=https://your-app.railway.app
     ```

---

## 🌐 Вариант 2: Render

### Преимущества:
- ✅ Бесплатный тариф (с ограничениями)
- ✅ Автоматический деплой
- ✅ HTTPS из коробки

### Шаги:

1. **Зарегистрируйтесь на Render:**
   - https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - New → Web Service
   - Подключите GitHub репозиторий
   - Настройки:
     - **Name:** `telegram-mini-app-backend`
     - **Root Directory:** `backend`
     - **Environment:** `Node`
     - **Build Command:** `npm install`
     - **Start Command:** `npm start`

3. **Добавьте переменные окружения:**
   - Environment Variables:
     ```
     PORT=3000
     DATABASE_URL=postgresql://... (ваш Supabase URL)
     RUN_MIGRATIONS=true
     ```

4. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически задеплоит
   - Получите URL: `https://your-app.onrender.com`

---

## 🐳 Вариант 3: Fly.io

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Глобальная сеть
- ✅ Быстрый деплой

### Шаги:

1. **Установите Fly CLI:**
   ```powershell
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите:**
   ```powershell
   fly auth login
   ```

3. **Создайте приложение:**
   ```powershell
   cd backend
   fly launch
   ```

4. **Настройте переменные:**
   ```powershell
   fly secrets set DATABASE_URL="postgresql://..."
   fly secrets set RUN_MIGRATIONS="true"
   ```

5. **Деплой:**
   ```powershell
   fly deploy
   ```

---

## 💻 Вариант 4: VPS (DigitalOcean, Hetzner, Vultr)

### Преимущества:
- ✅ Полный контроль
- ✅ Нет ограничений бесплатного тарифа
- ✅ Можно использовать PM2

### Шаги:

1. **Создайте VPS:**
   - DigitalOcean: https://www.digitalocean.com
   - Hetzner: https://www.hetzner.com
   - Vultr: https://www.vultr.com
   - Минимум: 1GB RAM, 1 CPU

2. **Подключитесь по SSH:**
   ```bash
   ssh root@your-server-ip
   ```

3. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

4. **Клонируйте репозиторий:**
   ```bash
   git clone your-repo-url
   cd telegram-mini-app/backend
   ```

5. **Установите зависимости:**
   ```bash
   npm install
   ```

6. **Создайте .env:**
   ```bash
   nano .env
   # Добавьте DATABASE_URL, PORT и т.д.
   ```

7. **Установите PM2:**
   ```bash
   npm install -g pm2
   ```

8. **Запустите через PM2:**
   ```bash
   pm2 start npm --name "backend" -- start
   pm2 save
   pm2 startup
   ```

9. **Настройте Nginx (для HTTPS):**
   ```bash
   sudo apt install nginx certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

---

## 📊 Сравнение платформ

| Платформа | Бесплатный тариф | Сложность | Рекомендация |
|-----------|------------------|-----------|--------------|
| **Railway** | ✅ 500ч/мес | ⭐ Легко | ⭐⭐⭐⭐⭐ |
| **Render** | ✅ (с ограничениями) | ⭐ Легко | ⭐⭐⭐⭐ |
| **Fly.io** | ✅ | ⭐⭐ Средне | ⭐⭐⭐ |
| **VPS** | ❌ (от $5/мес) | ⭐⭐⭐ Сложно | ⭐⭐⭐ |

---

## 🔧 После деплоя

### 1. Обновите frontend:

В настройках вашего frontend (Vercel/Netlify) добавьте переменную:

```
VITE_API_URL=https://your-backend-url.railway.app
```

### 2. Проверьте работу:

```bash
curl https://your-backend-url.railway.app/health
```

Должно вернуть: `{"ok":true}`

### 3. Проверьте в приложении:

- Откройте Telegram Mini App
- Проверьте консоль браузера (F12)
- Должен использоваться новый URL

---

## 💡 Рекомендация

**Для начала используйте Railway** - это самый простой способ:
- ✅ Регистрация через GitHub
- ✅ Автоматический деплой
- ✅ Бесплатный тариф
- ✅ HTTPS из коробки
- ✅ Простая настройка

---

## ❓ Вопросы?

Если возникли проблемы:
1. Проверьте логи в панели платформы
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что порт указан правильно
4. Убедитесь, что миграции выполнены
