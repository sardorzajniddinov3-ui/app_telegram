# Создание файла .env для Backend

## ⚠️ ПРОБЛЕМА
Backend не может запуститься, потому что файл `.env` не существует или содержит неправильный `DATABASE_URL`.

## ✅ РЕШЕНИЕ

### Шаг 1: Получите Connection String из Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите: **Settings** → **Database**
4. Найдите секцию **Connection string**
5. Выберите вкладку **URI**
6. Скопируйте строку подключения

**Пример строки:**
```
postgresql://postgres.abcdefghijklmnop:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

### Шаг 2: Создайте файл .env

**Способ 1: Через PowerShell (рекомендуется)**

Выполните в PowerShell (замените на ваш Connection String):

```powershell
cd "apptg\telegram-mini-app\backend"

@"
PORT=3000
DATABASE_URL=postgresql://postgres.xxxxx:[ВАШ-ПАРОЛЬ]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
RUN_MIGRATIONS=false
"@ | Out-File -FilePath ".env" -Encoding utf8
```

**Способ 2: Вручную**

1. Откройте папку `backend`
2. Создайте новый файл с именем `.env` (без расширения)
3. Добавьте следующее содержимое (замените на ваш Connection String):

```env
PORT=3000
DATABASE_URL=postgresql://postgres.xxxxx:[ВАШ-ПАРОЛЬ]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
RUN_MIGRATIONS=false
```

### Шаг 3: Замените значения

В файле `.env` замените:
- `[ВАШ-ПАРОЛЬ]` на пароль от базы данных Supabase
- `xxxxx` и остальную часть URL на ваш реальный Connection String

**Как найти пароль:**
- В Supabase Dashboard: **Settings** → **Database** → **Database password**
- Если забыли, можно сбросить там же

### Шаг 4: Проверьте файл

Убедитесь, что файл `.env` находится в папке `backend` и содержит правильный `DATABASE_URL`.

### Шаг 5: Запустите backend снова

```powershell
npm start
```

Теперь должно работать! ✅

---

## ❗ Важные замечания

1. **Файл `.env` должен быть в папке `backend`** (не в корне проекта)
2. **Не коммитьте `.env` в git** (он уже в .gitignore)
3. **Используйте формат URI** для Connection String
4. **RUN_MIGRATIONS=false** потому что миграция уже выполнена в Supabase

---

## 🔍 Проверка

После создания `.env` и запуска `npm start`, вы должны увидеть:

```
API listening on :3000
```

Если видите ошибки подключения:
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что пароль правильный
- Проверьте, что Supabase проект активен
