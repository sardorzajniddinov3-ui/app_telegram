# ✅ Проверка готовности к деплою на Railway

## Что было исправлено:

### ✅ 1. Использование `process.env.PORT`
- Сервер теперь читает порт из `process.env.PORT`
- Fallback на 3000 только для локальной разработки
- Railway автоматически устанавливает `PORT` при деплое

### ✅ 2. Слушание на правильном хосте
- Сервер слушает на `0.0.0.0` (принимает соединения извне)
- Это необходимо для Railway и других облачных платформ

### ✅ 3. Правильный запуск
- `npm start` → `node src/index.js` ✅
- Без nodemon в production ✅
- Без dev-режима ✅

### ✅ 4. Обработка ошибок
- Улучшена обработка ошибок при старте
- Graceful shutdown при остановке
- Логирование для отладки

### ✅ 5. Структура проекта
- `package.json` в корне `backend/` ✅
- `src/index.js` - точка входа ✅
- Все зависимости в `package.json` ✅

## Проверка перед деплоем:

### Локальная проверка:

```powershell
cd backend
npm install
npm start
```

Должно появиться:
```
Starting server...
PORT: 3000
Database pool created
✅ Server is running on port 3000
🌐 Health check: http://0.0.0.0:3000/health
```

### Проверка health endpoint:

```powershell
curl http://localhost:3000/health
```

Должно вернуть: `{"ok":true}`

## Переменные окружения для Railway:

В настройках Railway добавьте:

```
PORT=3000                    # Railway устанавливает автоматически, но можно указать явно
DATABASE_URL=postgresql://... # Ваш Supabase или другой PostgreSQL URL
RUN_MIGRATIONS=true          # Запускать миграции при старте
```

## Структура для Railway:

```
backend/                     ← Корневая папка сервиса
├── package.json            ← Должен быть здесь
├── railway.json            ← Конфигурация Railway
├── src/
│   ├── index.js            ← Точка входа
│   ├── app.js
│   └── ...
└── ...
```

## Команды Railway:

- **Build:** `npm install` (автоматически)
- **Start:** `npm start` → `node src/index.js`

## Что Railway делает автоматически:

1. ✅ Устанавливает `PORT` в переменные окружения
2. ✅ Запускает `npm install`
3. ✅ Запускает `npm start`
4. ✅ Проверяет health endpoint на `/health`

## Если что-то не работает:

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что порт не занят
4. Убедитесь, что все зависимости установлены

## Финальная версия `src/index.js`:

См. файл `src/index.js` - он готов для деплоя на Railway.
