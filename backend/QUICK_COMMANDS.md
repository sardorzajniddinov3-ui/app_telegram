# 🚀 Быстрые команды для работы с бэкендом

## Правильный путь к папке backend:

```
D:\apptg\apptg111\apptg111\apptg\telegram-mini-app\backend
```

## Команды для PowerShell:

### Переход в папку backend:

```powershell
cd D:\apptg\apptg111\apptg111\apptg\telegram-mini-app\backend
```

Или используйте скрипт:
```powershell
.\go-to-backend.ps1
```

### Установка зависимостей:

```powershell
npm install
```

### Запуск сервера:

```powershell
npm start
```

### Запуск в режиме разработки (с автоперезагрузкой):

```powershell
npm run dev
```

### Проверка health endpoint:

```powershell
curl http://localhost:3000/health
```

Или в браузере:
```
http://localhost:3000/health
```

## Структура проекта:

```
D:\apptg\apptg111\                    ← Корень проекта
└── apptg111\
    └── apptg\
        └── telegram-mini-app\
            ├── backend\               ← Папка бэкенда
            │   ├── package.json
            │   ├── src\
            │   │   └── index.js
            │   └── ...
            └── src\                   ← Папка фронтенда
```

## Важно:

- ✅ Всегда переходите в папку `backend` перед запуском команд
- ✅ Убедитесь, что файл `.env` создан и настроен
- ✅ Проверьте, что PostgreSQL/Supabase доступен
