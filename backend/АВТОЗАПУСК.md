# 🚀 Автозапуск бэкенда

## ⚠️ Важно понимать

**Локальный бэкенд работает только когда ваш ПК включен!**

- ✅ ПК включен → бэкенд работает
- ❌ ПК выключен → бэкенд не работает → приложение не работает

**Для работы 24/7 нужно развернуть бэкенд в облаке!**

📖 **Инструкция по деплою в облако:** См. [DEPLOY_CLOUD.md](./DEPLOY_CLOUD.md)

---

## Быстрый старт (1 команда)

**Откройте PowerShell от имени администратора** и выполните:

```powershell
cd D:\apptg\apptg111\apptg111\apptg\telegram-mini-app\backend
.\setup-autostart.ps1
```

Готово! Бэкенд будет запускаться автоматически при каждом включении компьютера.

## Что делает скрипт?

✅ Устанавливает PM2 (процесс-менеджер для Node.js)  
✅ Настраивает автозапуск через планировщик Windows  
✅ Запускает бэкенд прямо сейчас  
✅ Настраивает автоматический перезапуск при сбоях  

## Управление бэкендом

### Просмотр статуса
```powershell
pm2 status
```

### Просмотр логов
```powershell
pm2 logs telegram-mini-app-backend
```

### Перезапуск
```powershell
pm2 restart telegram-mini-app-backend
```

### Остановка
```powershell
pm2 stop telegram-mini-app-backend
```

### Запуск после остановки
```powershell
pm2 start telegram-mini-app-backend
```

## Проверка работы

После настройки автозапуска:

1. **Перезагрузите компьютер**
2. После входа проверьте:
   ```powershell
   pm2 status
   ```
3. Или откройте в браузере: `http://localhost:3000/api/tests`

## Устранение проблем

### Бэкенд не запускается

1. Проверьте логи:
   ```powershell
   pm2 logs telegram-mini-app-backend
   ```

2. Проверьте файл `.env`:
   - Убедитесь, что `DATABASE_URL` указан правильно
   - Убедитесь, что PostgreSQL запущен

3. Проверьте порт:
   - Убедитесь, что порт 3000 свободен
   - Или измените `PORT` в `.env`

### Удаление автозапуска

```powershell
pm2 delete telegram-mini-app-backend
pm2 unstartup
```

Или через планировщик задач Windows:
- Откройте `taskschd.msc`
- Найдите задачу `TelegramMiniAppBackend`
- Удалите её

## Альтернативные способы запуска

### Обычный запуск (без PM2)
```powershell
.\start-backend.ps1
```

### Запуск через PM2 (без автозапуска)
```powershell
.\start-backend-pm2.ps1
```

---

**Важно:** Перед настройкой автозапуска убедитесь, что:
- ✅ PostgreSQL установлен и запущен
- ✅ Файл `.env` создан и настроен
- ✅ Зависимости установлены (`npm install`)
