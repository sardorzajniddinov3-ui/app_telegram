# 🔄 Передеплой функции explain-answer

## Способ 1: Через Supabase Dashboard (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://supabase.com/dashboard
2. Войдите в свой аккаунт
3. Выберите ваш проект

### Шаг 2: Откройте Edge Functions
1. В левом меню найдите раздел **Edge Functions** (или **Functions**)
2. Найдите функцию **explain-answer** в списке
3. Нажмите на неё, чтобы открыть редактор

### Шаг 3: Обновите код
1. Скопируйте весь код из файла `index.ts` (убедитесь, что там `gemini-1.5-flash`)
2. Вставьте код в редактор в Dashboard
3. Нажмите кнопку **Deploy** или **Save and Deploy**
4. Дождитесь сообщения об успешном деплое

### Шаг 4: Проверка
После деплоя функция автоматически обновится и будет использовать новую модель `gemini-1.5-flash`

---

## Способ 2: Через Supabase CLI

### Если у вас установлен Supabase CLI:

1. Откройте терминал в папке проекта:
```bash
cd apptg111/apptg/telegram-mini-app
```

2. Войдите в Supabase (если еще не вошли):
```bash
supabase login
```

3. Свяжите проект (если еще не связан):
```bash
supabase link --project-ref ваш-project-ref
```

4. Задеплойте функцию:
```bash
supabase functions deploy explain-answer
```

---

## ⚠️ Важно!

После деплоя функция будет использовать модель **gemini-1.5-flash** вместо **gemini-2.5-flash**, что должно решить проблему с лимитом в 20 запросов в день.

Проверьте, что в коде функции используется:
```typescript
gemini-1.5-flash:generateContent
```

а не:
```typescript
gemini-2.5-flash:generateContent
```
