# 🚀 Настройка эмбеддингов для семантического поиска

## Что это дает?

Использование эмбеддингов `text-embedding-004` позволяет:
- ✅ **Находить похожие вопросы** - даже если формулировка немного отличается
- ✅ **Улучшить кэширование** - находить объяснения для семантически похожих вопросов
- ✅ **Экономить лимиты API** - использовать объяснения для похожих вопросов без запроса к ИИ

## Шаг 1: Установка расширения pgvector

1. Откройте **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Выполните:
   ```sql
   CREATE EXTENSION IF NOT EXISTS vector;
   ```

## Шаг 2: Обновление таблицы

1. В **SQL Editor** выполните SQL из файла `SUPABASE_AI_CACHE_EMBEDDINGS.sql`
2. Это добавит колонку `question_embedding` для хранения эмбеддингов

## Шаг 3: Создание функции векторного поиска

1. В **SQL Editor** выполните SQL из файла `SUPABASE_VECTOR_SEARCH_FUNCTION.sql`
2. Это создаст функцию `match_ai_explanations` для семантического поиска

## Шаг 4: Обновление Edge Function

1. Откройте **Edge Functions** → **explain-answer**
2. Скопируйте обновленный код из `index.ts`
3. Нажмите **Deploy**

## Как это работает?

1. **При сохранении объяснения:**
   - Создается эмбеддинг вопроса через `text-embedding-004`
   - Эмбеддинг сохраняется в колонке `question_embedding`

2. **При поиске объяснения:**
   - Сначала проверяется точное совпадение (как раньше)
   - Если не найдено, создается эмбеддинг текущего вопроса
   - Выполняется векторный поиск похожих вопросов
   - Если найдено похожее (схожесть > 80%), возвращается его объяснение

## Проверка работы

1. Добавьте несколько вопросов в тесты
2. Пройдите тест и получите объяснения
3. Измените формулировку вопроса (но сохраните смысл)
4. Пройдите тест снова - должно найтись объяснение из кэша

## Устранение проблем

### Ошибка: "extension vector does not exist"

**Решение:** Выполните `CREATE EXTENSION IF NOT EXISTS vector;` в SQL Editor

### Ошибка: "function match_ai_explanations does not exist"

**Решение:** Выполните SQL из файла `SUPABASE_VECTOR_SEARCH_FUNCTION.sql`

### Эмбеддинги не создаются

**Решение:** 
1. Проверьте, что `GEMINI_API_KEY` установлен в Secrets
2. Проверьте логи Edge Function в Supabase Dashboard
3. Убедитесь, что модель `text-embedding-004` доступна в вашем регионе

---

✅ **Готово!** Семантический поиск настроен и работает автоматически.
