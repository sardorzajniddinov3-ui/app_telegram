# 🔧 Исправление обрезанных URL изображений

## ❌ Проблема

URL изображений обрезаются при сохранении в базу данных:
- В базе сохраняется: `https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/qu` (117 символов)
- Должно быть: `https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/question-images/questions/123.webp` (полный URL)

## ✅ Решение

Изменена логика сохранения:
- **Раньше:** Сохранялся полный URL → обрезался в базе данных
- **Теперь:** Сохраняется только путь к файлу → полный URL формируется при чтении

### Что изменилось:

1. **Функция `uploadImageToStorage`** теперь возвращает только путь к файлу:
   - Вместо: `https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/question-images/questions/123.webp`
   - Возвращает: `questions/123.webp`

2. **Функция `resolveImage`** автоматически преобразует путь в полный URL при чтении

3. **Функция `deleteImageFromStorage`** обновлена для работы с обоими форматами (путь и полный URL)

## 🔄 Миграция существующих данных

### Вариант 1: Обновить обрезанные URL в базе данных

Если в базе есть обрезанные URL, их нужно исправить. Выполните SQL запрос:

```sql
-- Найти все обрезанные URL (которые заканчиваются на /qu или /question)
SELECT 
  id,
  question_text,
  image_url,
  LENGTH(image_url) as url_length
FROM questions
WHERE image_url LIKE 'https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/qu%'
  AND LENGTH(image_url) < 120
LIMIT 20;
```

Если обрезанные URL можно восстановить, обновите их:

```sql
-- ВНИМАНИЕ: Этот запрос нужно адаптировать под ваши данные!
-- Проверьте сначала, какие файлы есть в Storage

-- Пример: если в Storage есть файл questions/123.webp, а в базе обрезанный URL
-- Обновите вручную для каждого случая
UPDATE questions
SET image_url = 'questions/123.webp'  -- путь к файлу
WHERE id = 'ваш-id'
  AND image_url LIKE 'https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/qu%';
```

### Вариант 2: Удалить обрезанные URL

Если обрезанные URL нельзя восстановить:

```sql
-- Удалить обрезанные URL (установить в NULL)
UPDATE questions
SET image_url = NULL
WHERE image_url LIKE 'https://psjtbcotmnfvgulziara.supabase.co/storage/v1/object/public/qu%'
  AND LENGTH(image_url) < 120;
```

### Вариант 3: Оставить как есть

Если обрезанные URL не критичны, можно оставить их. При следующем редактировании вопроса с изображением, путь будет сохранен правильно.

## 📝 Проверка

### 1. Проверьте формат в базе данных

```sql
-- Проверьте, какие форматы сохранены
SELECT 
  id,
  question_text,
  image_url,
  CASE 
    WHEN image_url LIKE 'data:image/%' THEN 'Base64'
    WHEN image_url LIKE 'https://%' THEN 'Полный URL'
    WHEN image_url LIKE 'question-images/%' THEN 'Путь с bucket'
    WHEN image_url LIKE 'questions/%' THEN 'Путь без bucket (новый формат)'
    WHEN image_url IS NULL OR image_url = '' THEN 'Нет изображения'
    ELSE 'Неизвестный формат'
  END as image_format,
  LENGTH(image_url) as url_length
FROM questions
WHERE image_url IS NOT NULL AND image_url != ''
ORDER BY id DESC
LIMIT 20;
```

### 2. Проверьте, что новые изображения сохраняются правильно

После загрузки нового изображения проверьте в базе:
- Должен быть путь: `questions/123.webp` или `question-images/questions/123.webp`
- НЕ должен быть полный URL

### 3. Проверьте отображение

- Base64 изображения должны работать ✅
- Полные URL должны работать ✅ (для старых данных)
- Пути к файлам должны автоматически преобразовываться в полные URL ✅

## ✅ После исправления

1. **Новые изображения** будут сохраняться как пути к файлам (не обрезаются)
2. **Старые данные** продолжат работать (base64 и полные URL)
3. **Функция `resolveImage`** автоматически обработает все форматы

## ⚠️ Важно

- Поле `image_url` в базе данных имеет тип `TEXT`, но возможно есть ограничение на уровне API
- Теперь сохраняются только короткие пути, что решает проблему обрезания
- Полный URL формируется только при чтении через `resolveImage`
