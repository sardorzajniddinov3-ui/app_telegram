# 📱 Настройка доступа с мобильного устройства

## Проблема

При открытии приложения с телефона возникает ошибка подключения к серверу, потому что `localhost:3000` на телефоне указывает на сам телефон, а не на ваш компьютер.

## Решение

Код автоматически определяет, открыто ли приложение с мобильного устройства, и использует IP адрес вашего компьютера вместо `localhost`.

### Шаг 1: Узнайте IP адрес вашего компьютера

**Windows PowerShell:**
```powershell
ipconfig | Select-String "IPv4"
```

**Или через командную строку:**
```cmd
ipconfig
```

Найдите строку вида: `IPv4-адрес . . . . . . . . . . . . : 192.168.X.X`

### Шаг 2: Обновите IP адрес в коде

Откройте файлы:
- `src/App.jsx`
- `web/src/App.jsx`

Найдите строку:
```javascript
return 'http://192.168.39.35:3000';
```

Замените `192.168.39.35` на ваш IP адрес.

### Шаг 3: Убедитесь, что backend доступен по сети

Backend должен быть доступен не только на `localhost`, но и по IP адресу. Проверьте:

1. **Firewall Windows**: Разрешите входящие подключения на порт 3000
   ```powershell
   # Запустите PowerShell от имени администратора
   New-NetFirewallRule -DisplayName "Node.js Backend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
   ```

2. **Проверьте доступность:**
   - На компьютере: `http://localhost:3000/health`
   - С телефона (в той же Wi-Fi сети): `http://192.168.39.35:3000/health`

### Шаг 4: Альтернативный способ - через переменную окружения

Вы можете создать файл `.env.local` в папке `src` или `web`:

```env
VITE_BACKEND_URL=http://192.168.39.35:3000
```

Это переопределит автоматическое определение IP.

## Важные замечания

1. **IP адрес может измениться**: Если ваш компьютер переподключается к Wi-Fi, IP адрес может измениться. В этом случае нужно обновить его в коде.

2. **Одна и та же сеть Wi-Fi**: Телефон и компьютер должны быть в одной локальной сети Wi-Fi.

3. **Для продакшена**: В продакшене используйте доменное имя или статический IP адрес.

## Проверка работы

1. Запустите backend: `npm start` (в папке `backend`)
2. Запустите frontend: `npm run dev` (в папке `src` или `web`)
3. Откройте приложение на телефоне через Telegram Mini App
4. Проверьте консоль браузера (F12) - должны быть логи подключения к правильному адресу

## Решение проблем

### Ошибка "Failed to fetch" или "NetworkError"

- Убедитесь, что backend запущен
- Проверьте, что IP адрес правильный
- Проверьте firewall на компьютере
- Убедитесь, что телефон и компьютер в одной Wi-Fi сети

### Подписки не отображаются

- Проверьте консоль браузера (F12) - должны быть логи загрузки подписки
- Убедитесь, что Telegram User ID передается правильно в заголовках
- Проверьте, что backend возвращает правильные данные на `/api/subscription/me`
