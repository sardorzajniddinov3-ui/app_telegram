# 🚀 Запуск Backend - ИНСТРУКЦИЯ

## ⚠️ ВАЖНО: Backend должен быть запущен!

Все ошибки "Ошибка подключения к серверу" возникают потому, что backend не запущен.

## Быстрый запуск

### Шаг 1: Откройте терминал PowerShell

### Шаг 2: Перейдите в папку backend

```powershell
cd D:\apptg\apptg111\apptg111\apptg\telegram-mini-app\backend
```

### Шаг 3: Запустите backend

```powershell
npm start
```

## Что должно появиться:

```
Environment check:
  PORT: 3000
  DATABASE_URL: postgresql://...
  RUN_MIGRATIONS: true
Connecting to database...
✓ Database connection successful
Running migrations...
✓ Migrations completed
✓ API listening on 0.0.0.0:3000
✓ Health check: http://localhost:3000/health
✓ Available on network: http://<your-ip>:3000/health
```

## Проверка работы

### На компьютере:
```powershell
curl http://localhost:3000/health
```
Должно вернуть: `{"ok":true}`

### С телефона (в той же Wi-Fi сети):
Откройте в браузере: `http://192.168.39.35:3000/health`
Должно вернуть: `{"ok":true}`

## Если backend не запускается

### Ошибка подключения к БД:
1. Проверьте файл `.env` в папке `backend`
2. Убедитесь, что `DATABASE_URL` правильный (Supabase PostgreSQL)
3. Проверьте подключение к Supabase

### Порт занят:
1. Найдите процесс, использующий порт 3000:
   ```powershell
   netstat -ano | findstr :3000
   ```
2. Остановите процесс или измените PORT в `.env`

## Backend должен работать постоянно!

Не закрывайте терминал, пока работаете с приложением. Backend должен быть запущен все время.
